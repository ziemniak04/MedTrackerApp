openapi: 3.0.2
info:
  title: ''
  version: ''
paths:
  /api/medications/:
    get:
      operationId: listMedications
      description: 'API endpoint for viewing and managing medications.


        Provides standard CRUD operations via the Django REST Framework

        `ModelViewSet`, as well as a custom action for retrieving

        additional information from an external API (OpenFDA).'
      parameters: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Medication'
          description: ''
      tags:
      - api
    post:
      operationId: createMedication
      description: 'API endpoint for viewing and managing medications.


        Provides standard CRUD operations via the Django REST Framework

        `ModelViewSet`, as well as a custom action for retrieving

        additional information from an external API (OpenFDA).'
      parameters: []
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Medication'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/Medication'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/Medication'
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Medication'
          description: ''
      tags:
      - api
  /api/medications/{id}/:
    get:
      operationId: retrieveMedication
      description: 'API endpoint for viewing and managing medications.


        Provides standard CRUD operations via the Django REST Framework

        `ModelViewSet`, as well as a custom action for retrieving

        additional information from an external API (OpenFDA).'
      parameters:
      - name: id
        in: path
        required: true
        description: A unique integer value identifying this medication.
        schema:
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Medication'
          description: ''
      tags:
      - api
    put:
      operationId: updateMedication
      description: 'API endpoint for viewing and managing medications.


        Provides standard CRUD operations via the Django REST Framework

        `ModelViewSet`, as well as a custom action for retrieving

        additional information from an external API (OpenFDA).'
      parameters:
      - name: id
        in: path
        required: true
        description: A unique integer value identifying this medication.
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Medication'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/Medication'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/Medication'
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Medication'
          description: ''
      tags:
      - api
    patch:
      operationId: partialUpdateMedication
      description: 'API endpoint for viewing and managing medications.


        Provides standard CRUD operations via the Django REST Framework

        `ModelViewSet`, as well as a custom action for retrieving

        additional information from an external API (OpenFDA).'
      parameters:
      - name: id
        in: path
        required: true
        description: A unique integer value identifying this medication.
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Medication'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/Medication'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/Medication'
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Medication'
          description: ''
      tags:
      - api
    delete:
      operationId: destroyMedication
      description: 'API endpoint for viewing and managing medications.


        Provides standard CRUD operations via the Django REST Framework

        `ModelViewSet`, as well as a custom action for retrieving

        additional information from an external API (OpenFDA).'
      parameters:
      - name: id
        in: path
        required: true
        description: A unique integer value identifying this medication.
        schema:
          type: string
      responses:
        '204':
          description: ''
      tags:
      - api
  /api/medications/{id}/expected-doses/:
    get:
      operationId: expectedDosesMedication
      description: "Calculate expected doses for a medication over a given number\
        \ of days.\n\nQuery Parameters:\n    - days (int): Number of days (required,\
        \ must be a positive integer)."
      parameters:
      - name: id
        in: path
        required: true
        description: A unique integer value identifying this medication.
        schema:
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Medication'
          description: ''
      tags:
      - api
  /api/medications/{id}/info/:
    get:
      operationId: getExternalInfoMedication
      description: 'Retrieve external drug information from the OpenFDA API.


        Calls the `Medication.fetch_external_info()` method, which

        delegates to the `DrugInfoService` for API access.'
      parameters:
      - name: id
        in: path
        required: true
        description: A unique integer value identifying this medication.
        schema:
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Medication'
          description: ''
      tags:
      - api
  /api/logs/:
    get:
      operationId: listDoseLogs
      description: 'API endpoint for viewing and managing dose logs.


        A DoseLog represents an event where a medication dose was either

        taken or missed. This viewset provides standard CRUD operations

        and a custom filtering action by date range.'
      parameters: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/DoseLog'
          description: ''
      tags:
      - api
    post:
      operationId: createDoseLog
      description: 'API endpoint for viewing and managing dose logs.


        A DoseLog represents an event where a medication dose was either

        taken or missed. This viewset provides standard CRUD operations

        and a custom filtering action by date range.'
      parameters: []
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DoseLog'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/DoseLog'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/DoseLog'
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DoseLog'
          description: ''
      tags:
      - api
  /api/logs/filter/:
    get:
      operationId: filterByDateDoseLog
      description: "Retrieve all dose logs within a given date range.\n\nQuery Parameters:\n\
        \    - start (YYYY-MM-DD): Start date of the range (inclusive).\n    - end\
        \ (YYYY-MM-DD): End date of the range (inclusive)."
      parameters: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DoseLog'
          description: ''
      tags:
      - api
  /api/logs/{id}/:
    get:
      operationId: retrieveDoseLog
      description: 'API endpoint for viewing and managing dose logs.


        A DoseLog represents an event where a medication dose was either

        taken or missed. This viewset provides standard CRUD operations

        and a custom filtering action by date range.'
      parameters:
      - name: id
        in: path
        required: true
        description: A unique integer value identifying this dose log.
        schema:
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DoseLog'
          description: ''
      tags:
      - api
    put:
      operationId: updateDoseLog
      description: 'API endpoint for viewing and managing dose logs.


        A DoseLog represents an event where a medication dose was either

        taken or missed. This viewset provides standard CRUD operations

        and a custom filtering action by date range.'
      parameters:
      - name: id
        in: path
        required: true
        description: A unique integer value identifying this dose log.
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DoseLog'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/DoseLog'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/DoseLog'
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DoseLog'
          description: ''
      tags:
      - api
    patch:
      operationId: partialUpdateDoseLog
      description: 'API endpoint for viewing and managing dose logs.


        A DoseLog represents an event where a medication dose was either

        taken or missed. This viewset provides standard CRUD operations

        and a custom filtering action by date range.'
      parameters:
      - name: id
        in: path
        required: true
        description: A unique integer value identifying this dose log.
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DoseLog'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/DoseLog'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/DoseLog'
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DoseLog'
          description: ''
      tags:
      - api
    delete:
      operationId: destroyDoseLog
      description: 'API endpoint for viewing and managing dose logs.


        A DoseLog represents an event where a medication dose was either

        taken or missed. This viewset provides standard CRUD operations

        and a custom filtering action by date range.'
      parameters:
      - name: id
        in: path
        required: true
        description: A unique integer value identifying this dose log.
        schema:
          type: string
      responses:
        '204':
          description: ''
      tags:
      - api
  /api/notes/:
    get:
      operationId: listNotes
      description: 'API endpoint for viewing and managing notes.


        A Note is a text entry associated with a specific medication.

        This viewset provides list, create, retrieve, and delete operations.

        Update (PUT/PATCH) operations are not supported.'
      parameters:
      - name: search
        required: false
        in: query
        description: A search term.
        schema:
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Note'
          description: ''
      tags:
      - api
    post:
      operationId: createNote
      description: 'API endpoint for viewing and managing notes.


        A Note is a text entry associated with a specific medication.

        This viewset provides list, create, retrieve, and delete operations.

        Update (PUT/PATCH) operations are not supported.'
      parameters: []
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Note'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/Note'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/Note'
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Note'
          description: ''
      tags:
      - api
  /api/notes/{id}/:
    get:
      operationId: retrieveNote
      description: 'API endpoint for viewing and managing notes.


        A Note is a text entry associated with a specific medication.

        This viewset provides list, create, retrieve, and delete operations.

        Update (PUT/PATCH) operations are not supported.'
      parameters:
      - name: id
        in: path
        required: true
        description: A unique integer value identifying this note.
        schema:
          type: string
      - name: search
        required: false
        in: query
        description: A search term.
        schema:
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Note'
          description: ''
      tags:
      - api
    delete:
      operationId: destroyNote
      description: 'API endpoint for viewing and managing notes.


        A Note is a text entry associated with a specific medication.

        This viewset provides list, create, retrieve, and delete operations.

        Update (PUT/PATCH) operations are not supported.'
      parameters:
      - name: id
        in: path
        required: true
        description: A unique integer value identifying this note.
        schema:
          type: string
      - name: search
        required: false
        in: query
        description: A search term.
        schema:
          type: string
      responses:
        '204':
          description: ''
      tags:
      - api
components:
  schemas:
    Medication:
      type: object
      properties:
        id:
          type: integer
          readOnly: true
        name:
          type: string
          maxLength: 100
        dosage_mg:
          type: integer
          maximum: 2147483647
          minimum: 0
        prescribed_per_day:
          type: integer
          maximum: 2147483647
          description: Expected number of doses per day
          minimum: 0
        adherence:
          type: string
          readOnly: true
      required:
      - name
      - dosage_mg
      - prescribed_per_day
    DoseLog:
      type: object
      properties:
        id:
          type: integer
          readOnly: true
        medication:
          type: integer
        taken_at:
          type: string
          format: date-time
        was_taken:
          type: boolean
      required:
      - medication
      - taken_at
    Note:
      type: object
      properties:
        id:
          type: integer
          readOnly: true
        medication:
          type: integer
        text:
          type: string
        created_at:
          type: string
          format: date-time
          readOnly: true
      required:
      - medication
      - text
